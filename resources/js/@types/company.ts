export type Company = {
    id: string;
    name: string;
    email: string;
    phone: string;
    creation: string;
    contactPerson: string;
};

export type CompanyType = {
    id: string;
    name: string;
};

export type CompanyActivity = {
    id: string;
    name: string;
};
